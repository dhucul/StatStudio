<Window x:Class="StatStudio.Wpf.Dialogs.XyPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#1E1E1E" Foreground="#D4D4D4"
        Title="Scatterplot" Height="220" Width="340"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="Prompt" Grid.Row="0" Grid.ColumnSpan="2" Foreground="#9CDCFE"
                   Margin="0,0,0,12" Text="Choose the Y (response) and X (predictor) variables."/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Y variable:" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox x:Name="YCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="X variable:" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox x:Name="XCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Click="OnOk" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="6,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
