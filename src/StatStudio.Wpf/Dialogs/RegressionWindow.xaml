<Window x:Class="StatStudio.Wpf.Dialogs.RegressionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#1E1E1E" Foreground="#D4D4D4"
        Title="Multiple Regression" Height="460" Width="380"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Response (Y):" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="ResponseCombo" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Foreground="#9CDCFE" Margin="0,0,0,6"
                   Text="Predictors (X):"/>
        <Border Grid.Row="3" Grid.ColumnSpan="2" BorderBrush="#3C3C3C" BorderThickness="1" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="List" Margin="4"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Click="OnOk" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="6,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
