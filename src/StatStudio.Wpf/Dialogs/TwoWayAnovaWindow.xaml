<Window x:Class="StatStudio.Wpf.Dialogs.TwoWayAnovaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource Bg}" Foreground="{DynamicResource Text}"
        Title="Two-Way ANOVA" Height="240" Width="360"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Response (numeric):" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="ResponseCombo" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Factor A:" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="FactorACombo" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Factor B:" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="FactorBCombo" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Click="OnOk" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="6,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
